{
  "name": "ai-code-detector",
  "version": "0.2.0",
  "private": true,
  "description": "Dev tooling only. The app itself is static HTML/CSS/JS with no build step and no runtime dependencies — open index.html and it works.",
  "scripts": {
    "serve": "http-server -p 8899 -s",
    "audit:contrast": "node scripts/contrast-audit.mjs"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "playwright": "^1.56.1"
  }
}
